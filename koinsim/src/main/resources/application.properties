spring.application.name=koinsim

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/koinsim_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#JWT
jwt.secret=mysupersecretkey1234567890abcdef

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.session.store-type=redis

# La versione pubblica gratuita di CoinGecko funziona senza autenticazione, 
# quindi nell'application.properties non serve aggiungere nulla per le crypto.
# alpha vantage APIKey
alpha.vantage.api.key=946KJ68RKR54P6YS